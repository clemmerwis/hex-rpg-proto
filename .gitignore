# Ignore everything
*

# But not these files...
!*.html
!*.js
!*.css
!*.md
!*.json
!.gitignore
!*.yml
!Dockerfile

# Local Claude config (ignore even though .md is allowed)
.claude-config.md
.claude/settings.local.json

# Claude Code temporary working directory files (auto-ignored by *, but explicit for clarity)
# These are created during Claude Code sessions and should be cleaned up automatically
# Safe to delete when Claude Code is not running

# Track used assets
!*.jpg
!*.png

# Ignore unused sprite directories (must come after the negations)
sprites/Items/**

# ...even if they are in subdirectories
!*/
!.planning/