---
phase: 05-supporting-file-splits
plan: 02
type: execute
---

<objective>
Split InputHandler into exploration input and combat input handlers.

Purpose: InputHandler.js is 420 lines with combat input handling (attack mode clicks, combat key bindings, facing rotation) interleaved with exploration input (pathfinding clicks, hex markers, scrolling). Extracting combat input into a dedicated module separates the two modes cleanly.
Output: New js/CombatInputHandler.js module; InputHandler delegates combat-specific input to it.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan in this phase:
@.planning/phases/05-supporting-file-splits/05-01-PLAN.md

# Key source files (read AFTER 05-01 execution):
@js/InputHandler.js
@js/Game.js

**Constraints:**
- No behavior changes — all input must work identically
- Vanilla JS with ES6 modules
- Tab-4 indentation, double quotes, semicolons
- DI pattern: Game.js creates modules, injects via constructor or setDependencies()
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CombatInputHandler.js with combat-specific input methods</name>
  <files>js/CombatInputHandler.js</files>
  <action>
Create js/CombatInputHandler.js as a new ES6 module class. Extract combat input logic from InputHandler.js:

**Constructor signature:**
```js
constructor()
```
Initialize: `this.attackModeActive = false;`

**setDependencies(deps):**
Store: game, gameStateManager

**Methods to create (extract from InputHandler):**

1. `handleCombatClick(targetHex)` — extracted from handleMouseClick's combat branch (lines 123-135):
   - If attackModeActive: call gameStateManager.selectPlayerAttackTarget(), reset attackModeActive on success
   - Else: call gameStateManager.selectPlayerMoveTarget()
   - Returns true if handled (always true when called in combat state)

2. `handleCombatKeyDown(e)` — extracted from handleKeyDown's combat section (lines 178-235):
   - Space (no shift): skip turn if player hasn't acted
   - ArrowLeft/ArrowRight: rotate facing (Ctrl for 2 steps)
   - Key '1': set light attack, activate attack mode
   - Key '2': set heavy attack, activate attack mode
   - Enter: repeat last attack
   - Returns true if event was consumed (so InputHandler knows not to process further)

**Imports:** GAME_STATES from GameStateManager.js, rotateFacing from const.js
  </action>
  <verify>File js/CombatInputHandler.js exists with CombatInputHandler class exported, contains constructor, setDependencies, handleCombatClick, handleCombatKeyDown methods, proper imports</verify>
  <done>CombatInputHandler.js is a complete standalone module with all combat input logic</done>
</task>

<task type="auto">
  <name>Task 2: Wire CombatInputHandler into InputHandler and Game.js</name>
  <files>js/Game.js, js/InputHandler.js</files>
  <action>
**In Game.js:**
1. Import CombatInputHandler
2. Create instance:
   ```js
   this.combatInputHandler = new CombatInputHandler();
   ```
3. Set dependencies:
   ```js
   this.combatInputHandler.setDependencies({
       game: this.state,
       gameStateManager: this.gameStateManager
   });
   ```
4. Pass combatInputHandler to InputHandler via setDependencies

**In InputHandler.js:**
1. Remove `this.attackModeActive` from constructor (moved to CombatInputHandler)
2. Store combatInputHandler from setDependencies
3. In handleMouseClick, replace combat branch with delegation:
   ```js
   if (this.gameStateManager.currentState === GAME_STATES.COMBAT_INPUT) {
       this.combatInputHandler.handleCombatClick(targetHex);
       return;
   }
   ```
4. In handleKeyDown, replace combat key handling with delegation:
   - Move Space-skip and combat-specific keys into a combat check block:
   ```js
   if (this.gameStateManager.currentState === GAME_STATES.COMBAT_INPUT) {
       if (this.combatInputHandler.handleCombatKeyDown(e)) return;
   }
   ```
   - Keep Shift+Space (combat toggle) in InputHandler since it works from any state
   - Keep Tab prevention in InputHandler (used in both modes)
5. Remove `rotateFacing` import if only used by combat input (check — it was only used for facing rotation in combat)
6. Remove references to `this.attackModeActive` — it's now in CombatInputHandler

**InputHandler.js should retain:**
- Constructor (mouse state, keyboard state, callbacks, event setup, edge scrolling)
- setDependencies (with combatInputHandler added)
- setupEventListeners, handleMouseEnter, handleMouseLeave
- handleMouseMove (shared between modes)
- handleMouseClick — now thinner: hex marker check, combat delegation, exploration click
- handleKeyDown — now thinner: Tab prevention, Shift+Space combat toggle, combat delegation, non-combat keys
- handleKeyUp
- updateKeyboardScrolling, updateEdgeScrolling
- Hex marker mode methods (setHexMarkerMode, toggleMarkedHex, clearMarkedHexes, exportMarkedHexes)
- isShowAllNameplates, cleanup, debugCharacterPositions

That should be ~300 lines, down from 420.
  </action>
  <verify>Open browser at localhost:8080, test: (1) Exploration: click to move with pathfinding works, (2) Enter combat (Shift+Space), (3) Click adjacent hex to move in combat, (4) Press 1 for light attack then click to attack, (5) Press 2 for heavy attack, (6) Arrow keys rotate facing, (7) Space skips turn, (8) Enter repeats last attack, (9) Exit combat. No console errors.</verify>
  <done>CombatInputHandler owns combat input; InputHandler delegates combat state input; all input works identically; no console errors; Phase 5 complete</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Browser loads without errors at localhost:8080
- [ ] Exploration movement works (click to pathfind)
- [ ] Combat toggle works (Shift+Space)
- [ ] Combat move selection works (click adjacent hex)
- [ ] Combat attack mode works (press 1 or 2, then click)
- [ ] Facing rotation works (Arrow keys)
- [ ] Turn skip works (Space)
- [ ] Repeat attack works (Enter)
- [ ] Edge scrolling and keyboard scrolling work
- [ ] No console errors during full combat round
</verification>

<success_criteria>

- All tasks completed
- InputHandler.js reduced from 420 to ~300 lines
- CombatInputHandler.js exists as dedicated combat input module
- All input behavior is identical to before
- No console errors
- Phase 5 complete — CombatUILog and InputHandler split into focused modules
</success_criteria>

<output>
After completion, create `.planning/phases/05-supporting-file-splits/05-02-SUMMARY.md`
</output>
