---
phase: 01-constants-utilities
plan: 01
type: execute
---

<objective>
Move scattered configuration values to const.js — specifically COMBAT_TAGS/WRAPPER_TAGS from CombatUILog.js and viewport dimensions from Game.js.

Purpose: Eliminate configuration scattering so all tunable values live in one canonical location (const.js), matching the established codebase pattern.
Output: const.js gains two new exports (COMBAT_TAGS, WRAPPER_TAGS) and viewport dimensions in GAME_CONSTANTS. CombatUILog.js and Game.js import them instead of defining locally.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md

@js/const.js
@js/CombatUILog.js
@js/Game.js

**Constraining decisions:**
- No behavior changes — game must play identically before and after
- Vanilla JS only, no new dependencies
- Tab-4 indentation, double quotes, semicolons, PascalCase files
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move COMBAT_TAGS and WRAPPER_TAGS to const.js</name>
  <files>js/const.js, js/CombatUILog.js</files>
  <action>
Move the COMBAT_TAGS object (lines 7-16) and WRAPPER_TAGS object (lines 22-35) from CombatUILog.js to const.js. Export both as named exports.

In const.js: Add both objects near the bottom of the file (after FACTIONS or similar UI-related config). Keep the existing JSDoc comments that describe their purpose.

In CombatUILog.js: Remove the two object definitions (lines 7-35). Add COMBAT_TAGS and WRAPPER_TAGS to the existing import from './const.js' on line 1.

Do NOT change any values, keys, or HTML content within the tag objects — pure relocation only.
  </action>
  <verify>Grep for COMBAT_TAGS and WRAPPER_TAGS — should appear in const.js (definition + export) and CombatUILog.js (import + usage), nowhere else as definitions.</verify>
  <done>COMBAT_TAGS and WRAPPER_TAGS defined and exported from const.js. CombatUILog.js imports them. No duplicate definitions exist.</done>
</task>

<task type="auto">
  <name>Task 2: Move viewport dimensions to GAME_CONSTANTS</name>
  <files>js/const.js, js/Game.js</files>
  <action>
Add VIEWPORT_WIDTH: 1280 and VIEWPORT_HEIGHT: 720 to the GAME_CONSTANTS object in const.js, in the "World and rendering" section (near WORLD_WIDTH/WORLD_HEIGHT).

In Game.js constructor (lines 25-28), replace the hardcoded viewport object:
```
viewport: {
    width: 1280,
    height: 720
}
```
with:
```
viewport: {
    width: GAME_CONSTANTS.VIEWPORT_WIDTH,
    height: GAME_CONSTANTS.VIEWPORT_HEIGHT
}
```

GAME_CONSTANTS is already imported in Game.js line 14, so no import changes needed.
  </action>
  <verify>Grep for "1280" and "720" in js/Game.js — should no longer appear as hardcoded values. Grep VIEWPORT_WIDTH in const.js — should exist.</verify>
  <done>Viewport dimensions defined in GAME_CONSTANTS. Game.js references them instead of hardcoding. No other files affected.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] No hardcoded COMBAT_TAGS or WRAPPER_TAGS definitions remain in CombatUILog.js
- [ ] No hardcoded 1280/720 viewport values remain in Game.js constructor
- [ ] const.js exports COMBAT_TAGS, WRAPPER_TAGS, and GAME_CONSTANTS includes VIEWPORT_WIDTH/HEIGHT
- [ ] Open game in browser (http://localhost:8080), enter combat (Shift+Space), attack an enemy — combat log renders with correct colors and formatting
- [ ] Canvas renders at correct viewport size
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors in browser console
- Combat log formatting identical to before
- Canvas viewport dimensions identical to before
</success_criteria>

<output>
After completion, create `.planning/phases/01-constants-utilities/01-01-SUMMARY.md`
</output>
