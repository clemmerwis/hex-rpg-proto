---
phase: 02-combatsystem-pipeline
type: execute
---

<objective>
Extract pure calculation helpers from executeAttack() — hit resolution, resistance/vulnerability, flanking, DR, and crit logic become standalone methods on CombatSystem.

Purpose: Isolate side-effect-free damage pipeline stages so they can be understood, modified, and eventually tested independently. This is the foundation layer — later plans extract presentation and orchestration on top of these.
Output: CombatSystem.js with 3-4 new private methods; executeAttack() calls them instead of inline code.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context (dependency graph):
@.planning/phases/01-constants-utilities/01-02-SUMMARY.md

# Key source files:
@js/CombatSystem.js
@js/const.js (imports: calculateDamage, calculateAttackRating, calculateDefenseRating, calculateCSC, getEquipmentBonus, WEAPONS, ARMOR_TYPES, ATTACK_TYPES, STAT_BONUSES, DAMAGE_TYPE_PROPERTIES, isFlanking, getFacingFromDelta)

# Existing refactoring reference (use as structural guidance only — code examples are outdated):
@mdplans/current/combatsystem-refactoring-plan.md

**Established patterns:** hexKey(q, r) for hex coordinate keys; all constants in const.js
**Constraining decisions:**
- No behavior changes — game must play identically
- Vanilla JS only, tab-4 indentation, double quotes
- No new files — extracted methods stay as CombatSystem class methods
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract resolveHitRoll and applyResistanceModifier methods</name>
  <files>js/CombatSystem.js</files>
  <action>
Extract two new methods from executeAttack():

**resolveHitRoll(attacker, defender):**
- Moves lines 49-62 of current executeAttack (calculateAttackRating, calculateDefenseRating, evasionBonus, THC calc, d100 roll, display inversion) into a dedicated method
- Returns object: `{ hit, thc, thcRoll, thcPercent, rollPercent }`
- Pure calculation — no side effects, no logging

**applyResistanceModifier(damage, weapon, armor, attackType):**
- Moves lines 79-93 (resist/vuln check including enhanced vulnerability multipliers 2.0/2.5 for light/heavy) into a dedicated method
- Returns object: `{ damage: modifiedDamage, resistMod }` where resistMod is null | 'resistant' | 'vulnerable' | 'vulnerableEnhanced'
- Pure calculation — no side effects

Update executeAttack() to call these methods in place of the inline code. Preserve identical behavior — same variable names for downstream code that uses the results.

Do NOT extract flanking/DR or crit yet — that's Task 2.
  </action>
  <verify>
Grep CombatSystem.js for `resolveHitRoll` and `applyResistanceModifier` method definitions. Verify executeAttack() calls them. Verify no duplicate logic remains inline. Verify the resist/vuln enhanced multiplier values (2.0 for light vulnerableEnhancementLight, 2.5 for heavy vulnerableEnhancementHeavy) are preserved in the extracted method.
  </verify>
  <done>resolveHitRoll() and applyResistanceModifier() exist as methods; executeAttack() calls them; zero behavior change; enhanced vuln multipliers preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Extract calculateFlankingAndDR and applyCritModifier methods</name>
  <files>js/CombatSystem.js</files>
  <action>
Extract two more methods from executeAttack():

**calculateFlankingAndDR(attacker, defender, damage, armor):**
- Moves lines 97-117 (isFlanking check, canEngageBack check, effective DR with flanking modifier, DR absorption) into a dedicated method
- Returns object: `{ flanking, behindDefender, cannotEngageBack, effectiveDR, drAbsorbed, damage: damageAfterDR }`
- Uses this.gameStateManager.canEngageBack() and this.hexGrid — not pure but side-effect-free (read-only queries)

**applyCritModifier(attacker, defender, damage):**
- Moves lines 119-136 (CSC calculation, d100 roll, crit doubling, critMultiplier from equipment passives, display inversion) into a dedicated method
- Returns object: `{ crit, csc, cscRoll, cscPercent, cscRollPercent, damage: finalDamage }`
- Note: this has a random roll (d100) so it's not deterministic, but it's still isolated

Update executeAttack() to call these methods. The damage pipeline in executeAttack() should now read as a clear sequence:
1. baseDamage = calculateDamage(...)
2. { damage, resistMod } = this.applyResistanceModifier(baseDamage, weapon, armor, attackType)
3. { flanking, effectiveDR, drAbsorbed, damage } = this.calculateFlankingAndDR(attacker, defender, damage, armor)
4. { crit, damage: finalDamage, ... } = this.applyCritModifier(attacker, defender, damage)

Preserve identical behavior. Keep `damageAfterResist` and `damageAfterDR` intermediates accessible for the combat log (they're used in the damage breakdown string building that will be extracted in Plan 02-02). Store them as local variables in executeAttack() after each call.
  </action>
  <verify>
Grep CombatSystem.js for all four extracted method definitions (resolveHitRoll, applyResistanceModifier, calculateFlankingAndDR, applyCritModifier). Verify the damage pipeline sequence in executeAttack() follows the 4-step flow. Verify no inline calculation logic remains for these stages. Count total lines of executeAttack() — should be noticeably shorter but still contains log building and result handling inline.
  </verify>
  <done>All four calculation methods extracted; executeAttack() damage pipeline reads as clear 4-step sequence; zero behavior change; intermediate values (damageAfterResist, damageAfterDR) still available for log building.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All four methods exist: resolveHitRoll, applyResistanceModifier, calculateFlankingAndDR, applyCritModifier
- [ ] executeAttack() calls all four methods (no inline calculation logic for these stages)
- [ ] Damage pipeline is a clear sequential flow in executeAttack()
- [ ] No duplicate logic between extracted methods and executeAttack()
- [ ] Enhanced vulnerability multipliers preserved (2.0/2.5)
- [ ] CSC display inversion preserved (100 - csc, 101 - cscRoll)
- [ ] THC display inversion preserved (100 - thc, 101 - thcRoll)
- [ ] No new imports needed (all existing imports sufficient)
- [ ] Game loads in browser without console errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors introduced
- executeAttack() calculation logic extracted into 4 focused methods
- Identical game behavior preserved
</success_criteria>

<output>
After completion, create `.planning/phases/02-combatsystem-pipeline/02-01-SUMMARY.md`
</output>
