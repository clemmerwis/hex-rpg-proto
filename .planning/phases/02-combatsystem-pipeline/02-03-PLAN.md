---
phase: 02-combatsystem-pipeline
type: execute
---

<objective>
Extract result handlers from executeAttack() and rewrite it as a clean pipeline orchestrator — the final step in decomposing the ~200-line monolith.

Purpose: With calculation helpers (Plan 02-01) and presentation helpers (Plan 02-02) extracted, the remaining inline code is early-return handlers (whiff, miss) and the defeat/animation logic. Extracting these completes the decomposition, leaving executeAttack() as a readable ~35-45 line orchestrator.
Output: CombatSystem.js with executeAttack() as a clean pipeline; all logic in focused helper methods.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plans in this phase:
@.planning/phases/02-combatsystem-pipeline/02-01-SUMMARY.md
@.planning/phases/02-combatsystem-pipeline/02-02-SUMMARY.md

# Key source files:
@js/CombatSystem.js
@js/GameStateManager.js (for markCharacterHit reference)

**Established patterns:**
- Calculation methods return data objects (resolveHitRoll, applyResistanceModifier, calculateFlankingAndDR, applyCritModifier)
- Presentation methods return strings/arrays or call logger directly (formatAttackTypeName, buildDamageBreakdown, buildCombatLogLines, logDamageApplication)
**Constraining decisions:**
- No behavior changes — identical game play
- Vanilla JS, tab-4, double quotes
- Return value shape of executeAttack() must not change (callers depend on { hit, damage, crit, defenderDefeated, whiff?, flanking? })
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract handleWhiff, handleMiss, and handleHitResult methods</name>
  <files>js/CombatSystem.js</files>
  <action>
Extract three result-handling methods:

**handleWhiff(attacker, targetHex, attackTypeName):**
- Moves the whiff block (empty hex attack): log the whiff message, return the whiff result object
- Returns `{ hit: false, damage: 0, crit: false, defenderDefeated: false, whiff: true }`
- Uses formatAttackTypeName with verb='attacks' for the whiff log line
- Calls this.logger.combat() with the whiff message

**handleMiss(attacker, defender, attackTypeName, hitResult):**
- Moves the miss block: log the miss message with THC/roll data, return miss result
- Returns `{ hit: false, damage: 0, crit: false, defenderDefeated: false }`
- hitResult provides thcPercent, rollPercent for the log message
- Calls this.logger.combat()

**handleHitResult(attacker, defender, finalDamage, crit, flanking):**
- Moves lines 204-219 (defeat check, markCharacterHit, impact animation, return) into a method
- Calls this.gameStateManager.markCharacterHit(defender)
- If defeated: logs defeat message, returns result with defenderDefeated: true
- If not defeated and finalDamage > 0: sets defender.currentAnimation = 'impact', defender.animationFrame = 0
- Returns `{ hit: true, damage: finalDamage, crit, defenderDefeated: defeated, flanking }`

Update executeAttack() to use these handlers for all three code paths (whiff → handleWhiff, miss → handleMiss, hit → handleHitResult).
  </action>
  <verify>
Grep for handleWhiff, handleMiss, handleHitResult method definitions. Verify executeAttack() uses `return this.handleWhiff(...)`, `return this.handleMiss(...)`, and `return this.handleHitResult(...)`. Verify the return value shapes match the current return statements exactly (including whiff and flanking properties).
  </verify>
  <done>Three result handlers extracted; all return paths in executeAttack() go through handler methods; return value shapes identical.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up executeAttack as pipeline orchestrator</name>
  <files>js/CombatSystem.js</files>
  <action>
Review and clean up executeAttack() so it reads as a clear pipeline:

1. Face target
2. Lookup defender → if empty, return handleWhiff()
3. Format attack name
4. Check friendly fire (log warning if true)
5. Resolve hit roll → if miss, return handleMiss()
6. Get weapon and armor
7. Calculate base damage
8. Apply resistance modifier
9. Calculate flanking and DR
10. Apply crit modifier
11. Build combat log lines → output them
12. Apply damage (buffer system)
13. Log damage application
14. Return handleHitResult()

Each step should be 1-3 lines. The method should be ~35-45 lines total — a readable orchestration of the pipeline. Remove any leftover commented code, unnecessary intermediate variables, or dead code from the extraction process.

Do NOT change behavior. Do NOT rename the method. Do NOT change its parameters or return type. This is purely making the existing extracted code read cleanly.

Add a brief JSDoc summary listing the pipeline stages (similar to the existing comment block but updated to reflect the extracted method names).
  </action>
  <verify>
Count lines in executeAttack() — should be ~35-45 lines (down from ~200). Verify all 14 pipeline steps are visible as clear sequential calls. Verify no inline calculation, string building, or multi-line log construction remains. Verify the JSDoc lists the pipeline stages. Check that the overall CombatSystem.js file has grown (more methods) but each method is focused and short.
  </verify>
  <done>executeAttack() is a clean ~35-45 line pipeline orchestrator; all 10+ helper methods are focused and short; CombatSystem.js is well-structured; zero behavior change.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] executeAttack() is ~35-45 lines (orchestration only)
- [ ] All helper methods: resolveHitRoll, applyResistanceModifier, calculateFlankingAndDR, applyCritModifier, formatAttackTypeName, buildDamageBreakdown, buildCombatLogLines, logDamageApplication, handleWhiff, handleMiss, handleHitResult
- [ ] Return value shapes unchanged for all three paths (whiff, miss, hit)
- [ ] No leftover dead code or commented blocks from extraction
- [ ] applyDamage() and isValidAttackTarget() unchanged
- [ ] Game loads in browser without console errors
- [ ] Phase 2 complete — CombatSystem pipeline fully decomposed
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors introduced
- executeAttack() decomposed from ~200 lines to ~35-45 line pipeline orchestrator
- 10+ focused helper methods with clear single responsibilities
- Phase 2: CombatSystem Pipeline complete
</success_criteria>

<output>
After completion, create `.planning/phases/02-combatsystem-pipeline/02-03-SUMMARY.md`

Note: This is the final plan in Phase 2. Success criteria for this plan includes "Phase 2 complete."
Update ROADMAP.md to mark Phase 2 as complete. Update STATE.md to advance to Phase 3.
</output>
