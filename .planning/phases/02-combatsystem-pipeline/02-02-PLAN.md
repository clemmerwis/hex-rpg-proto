---
phase: 02-combatsystem-pipeline
type: execute
---

<objective>
Extract presentation and logging helpers from executeAttack() — attack name formatting, damage breakdown building, combat log header assembly, and buffer damage logging become standalone methods.

Purpose: Separate the complex string-building logic (semantic tokens, tooltips, conditional formatting) from the combat pipeline flow. This is the most tangled part of executeAttack() — ~45 lines of interleaved string concatenation with combat state.
Output: CombatSystem.js with 4 new presentation methods; executeAttack() log-building section replaced with method calls.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan in this phase:
@.planning/phases/02-combatsystem-pipeline/02-01-SUMMARY.md

# Key source files:
@js/CombatSystem.js
@js/const.js (STAT_BONUSES, ATTACK_TYPES, WEAPONS used in damage breakdown)

**Established patterns:** Extracted methods return data objects; executeAttack() assigns results to local variables
**Constraining decisions:**
- No behavior changes — combat log output must be identical
- Semantic token format: {{tag}}content{{/tag}}, {{char:name}}, {{weapon:key}}, {{armor:key}}, {{tip:formula}}content{{/tip}}
- Vanilla JS, tab-4, double quotes
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract formatAttackTypeName and buildDamageBreakdown methods</name>
  <files>js/CombatSystem.js</files>
  <action>
Extract two presentation methods:

**formatAttackTypeName(weaponKey, attackType):**
- Moves lines 31-33 logic (heavy prefix + weapon token + " Attack") into a method
- Returns the formatted string, e.g. `{{heavy}}heavy{{/heavy}} {{weapon:longSword}} Attack`
- Also extract the whiff variant (line 37): add optional parameter `verb = 'Attack'` so whiff can call with `'attacks'`
- Pure string building

**buildDamageBreakdown(attacker, attackType, weapon, armor, baseDamage, damageAfterResist, resistMod, effectiveDR, flanking, drAbsorbed, damageAfterDR, crit, finalDamage):**
- Moves lines 148-179 (the complex damage breakdown string with {{tip}}, {{dmg}}, {{resist}}, {{vuln}}, {{dr}}, {{armor:key}} tokens) into a dedicated method
- Returns the complete damage breakdown string
- This is the most complex extraction — the string building references: STAT_BONUSES.MULTIPLIER, weapon.force, weapon.base, ATTACK_TYPES[attackType].damageMod, attacker.stats.str, attacker.equipment.mainHand, defender.equipment.armor, armor.flankingDefense
- Pass all needed values as parameters rather than making the method re-derive them. The caller (executeAttack) already has these values from the pipeline stages extracted in Plan 02-01.
- Preserve exact string format including: camelCase-to-hyphen weapon name conversion, the {{tip:formula}} tooltip wrapping, conditional resist/vuln/vulnerableEnhanced blocks, conditional DR block with flanking percentage, conditional crit block

Update executeAttack() to call these methods. The attack name and damage breakdown sections should each become single method calls.
  </action>
  <verify>
Grep for `formatAttackTypeName` and `buildDamageBreakdown` method definitions. Verify executeAttack() calls formatAttackTypeName for both normal attack name (line 33) and whiff attack name (line 37). Verify the damage breakdown method handles all four conditional blocks: resist, vuln, vuln+enhanced, DR with flanking, and crit.
  </verify>
  <done>formatAttackTypeName() and buildDamageBreakdown() exist as methods; executeAttack() calls them; combat log output identical; all conditional formatting paths preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Extract buildCombatLogLines and logDamageApplication methods</name>
  <files>js/CombatSystem.js</files>
  <action>
Extract two more presentation methods:

**buildCombatLogLines(attacker, defender, attackTypeName, thcPercent, rollPercent, crit, flanking, friendlyFire, cscPercent, cscRollPercent, damageBreakdown):**
- Moves lines 141-184 logic (assembling the multi-line combat log: header line with THC/roll/hit, optional crit/flanking/friendlyFire tags, CSC line if crit, damage breakdown line) into a method
- Returns an array of log strings that executeAttack() passes to logger.combat() in sequence
- This consolidates the scattered logger.combat() calls (lines 182-184) into a single returned structure

**logDamageApplication(defender, attacker, damageResult):**
- Moves lines 189-202 (the context-sensitive buffer/HP damage log output with {{buf}}, {{buf_depleted}}, {{buf_bypassed}}, {{hp}} tokens) into a method
- Takes the result object from applyDamage() as parameter (bufferDamage, healthDamage, bufferBefore, bufferAfter, healthBefore, healthAfter, bypassed)
- Calls this.logger.combat() directly (this is a side-effect method, unlike the pure builders above)

Update executeAttack() to replace the scattered log lines with:
1. `const logLines = this.buildCombatLogLines(...)`
2. `logLines.forEach(line => this.logger.combat(line))`
3. `this.logDamageApplication(defender, attacker, damageResult)`
  </action>
  <verify>
Grep for `buildCombatLogLines` and `logDamageApplication` method definitions. Verify executeAttack() no longer has direct logger.combat() calls for the hit path (except through these methods). Count remaining direct logger.combat() calls in executeAttack() — should only be the whiff and miss paths (which will be extracted in Plan 02-03).
  </verify>
  <done>buildCombatLogLines() and logDamageApplication() exist; hit-path combat logging consolidated into method calls; executeAttack() log section reduced from ~20 scattered lines to 3 clean lines.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All four presentation methods exist: formatAttackTypeName, buildDamageBreakdown, buildCombatLogLines, logDamageApplication
- [ ] executeAttack() hit-path logging reduced to method calls
- [ ] Semantic token output identical ({{char:}}, {{weapon:}}, {{armor:}}, {{tip:}}, {{dmg}}, {{hit}}, {{miss}}, {{resist}}, {{vuln}}, {{dr}}, {{buf}}, {{hp}}, etc.)
- [ ] Tooltip formula format preserved ({{tip:weapon_dmg: X + ...}})
- [ ] Buffer damage log preserves all 4 conditional branches (bypassed, overflow, buffer-only, health-only)
- [ ] No new imports needed
- [ ] Game loads in browser without console errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors introduced
- Combat log output identical to pre-refactoring
- executeAttack() presentation logic extracted into 4 focused methods
</success_criteria>

<output>
After completion, create `.planning/phases/02-combatsystem-pipeline/02-02-SUMMARY.md`
</output>
